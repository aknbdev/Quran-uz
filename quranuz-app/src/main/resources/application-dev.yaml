server.port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/io_quran_db
    username: io_quran_user
    password: rootan
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate.ddl-auto: update